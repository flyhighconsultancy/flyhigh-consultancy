FLY HIGH CONSULTANCY — FLAGS FIXED

Country flag PNGs are embedded directly into index.html as data URIs.
Deploy the FlyHighConsultancy_FLAGS_FIXED folder itself to Vercel Drop.
Do not upload the ZIP and do not upload its parent folder.
